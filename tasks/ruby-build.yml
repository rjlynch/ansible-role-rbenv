---
- name: Сloning ruby-build plugin for rbenv
  become: true
  become_user: "{{rbenv_user}}"
  git: repo=https://github.com/sstephenson/ruby-build.git
       dest={{ rbenv_root_path }}/plugins/ruby-build
       update=no